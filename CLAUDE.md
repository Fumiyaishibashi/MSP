# MBS Synergy Platform - アプリケーション概要

## 📋 プロジェクト概要

**MBS Synergy Platform** は、MBSグループ（日本の大手放送局）向けのコラボレーションプラットフォームです。

### 目的

業界イベント企画において、社内に散在する**IP資産（番組、キャラクター、技術リソースなど）**を視覚的に発見・組み合わせ、新しいビジネス機会を創出することを支援します。

### コンセプト

「デジタルホワイトボード」として機能し、ユーザーが付箋紙をボードに貼り付けるように、直感的なドラッグ&ドロップでIP資産を配置・組み合わせることで、セレンディピティな発見とコラボレーションを促進します。

---

## ⚠️ **重要: フロントエンドのみの実装**

### **このアプリケーションは完全にフロントエンドで閉じています**

| 項目 | 状態 |
|------|------|
| **バックエンド API** | ❌ **なし** |
| **データベース** | ❌ **なし** |
| **サーバーサイド処理** | ❌ **なし** |
| **外部連携** | ❌ **なし** |

**すべてのデータは以下の方法で管理されます：**
- 📍 **メモリ上の状態** - React Context API
- 💾 **ブラウザのlocalStorage** - ページリロード時に復元

**つまり：** デモ・プレゼンテーション専用のスタンドアロン・アプリケーションです。データの永続化はブラウザレベルでのみ行われます。

---

## 🎯 主な機能

### 1. ダッシュボード (`/`)
- **5つの企画イベント案をグリッド表示**
- ステータスバッジ表示（募集中・企画中・準備中・終了）
- 「新規イベント作成」ボタン
- 「デモリセット」ボタン（初期状態に戻す）
- クリックで各イベントのキャンバスに遷移

### 2. イベントキャンバス (`/event/:id`)
**コア体験 - ドラッグ&ドロップで4種類のノートを配置**

#### 操作方法
| 操作 | 説明 |
|------|------|
| **ドラッグ** | サイドバーからIP資産・人メモ・会社メモをキャンバスに配置 |
| **リサイズ** | ノートの枠をドラッグして大きさ変更（react-rnd） |
| **クリック** | テキストを編集（IP メモ欄、アイデア内容） |
| **クリック（人/会社）** | 詳細情報モーダルを表示 |
| **「チャットを開く」** | メモ専用のオープンチャットスペースを開く |
| **削除ボタン** | ノートをワンクリック削除（確認なし） |

#### 4種類のノート

**IP ノート（黄色）**
- IP資産の情報を表示（ロゴ、名前、所有部門）
- メモ欄に自由にコメント記入可能
- Tailwind色: `bg-yellow-200`

**アイデア ノート（ピンク）**
- 自由形式のテキストメモ
- クリックで即座に編集可能
- Tailwind色: `bg-pink-200`

**人メモ ノート（青色）** ⭐ **NEW**
- 組織内の人物情報表示
- 名前、部門、専門領域を表示
- クリックで詳細情報モーダルを表示
- メール・電話番号のコピー機能
- オープンチャット対応
- Tailwind色: `bg-blue-200`

**会社メモ ノート（緑色）** ⭐ **NEW**
- 外部企業・パートナー企業情報表示
- 企業名、得意分野、推薦者リストを表示
- クリックで詳細情報モーダルを表示
- 推薦者・担当者の連絡先表示
- オープンチャット対応
- Tailwind色: `bg-emerald-200`

### 3. サイドバー（資産ライブラリ）

**3つのセクション：IP資産 + 人メモ + 会社メモ**

#### 📺 IP資産（既存）
**3つのカテゴリで12個のIP資産を提供**
- 番組IP: ヤングタウン、プレバト！！、ごぶごぶ、ごぶごぶラジオ、情熱大陸、あれみた、せやねん、ガンダム
- キャラクター/タレント: らいよんチャン、カピバラさん、福島暢啓アナウンサー
- 技術/リソース: VR撮影チーム、グッズ制作部、キッチンカー手配

**機能：** カテゴリ別に折りたたみ表示。各資産はドラッグ可能で、キャンバスに自由に配置できます。

#### 👤 人メモ（NEW）
**5人の社内メンバー情報**
- 鈴木太郎（グッズ制作部）
- 田中花子（ラジオ制作部）
- 佐藤次郎（テレビ制作部）
- 山本美咲（事業局）
- 伊藤健一（技術局）

**機能：** 青色タイルでドラッグ可能。名前と部門を表示し、キャンバスに配置するとプロフィール表示可能。

#### 🏢 会社メモ（NEW）
**5社の外部企業・パートナー情報**
- 〇〇デザイン（グラフィック・Web制作）
- イベントプラス（イベント運営・ステージ制作）
- グッズメイカーズ（グッズ製造・商品企画）
- ビデオテック（映像制作・4K撮影）
- ケータリング&フード（キッチンカー・イベント飲食）

**機能：** 緑色タイルでドラッグ可能。企業名と得意分野を表示し、キャンバスに配置すると推薦者・担当者リスト表示可能。

---

## 🏗️ 技術アーキテクチャ

### フレームワーク・ライブラリ

```json
{
  "core": {
    "React": "19.2.0",
    "TypeScript": "5.9.3",
    "Vite": "7.2.4 (Build tool)"
  },
  "routing": {
    "React Router DOM": "7.9.6"
  },
  "styling": {
    "Tailwind CSS": "3.4.4",
    "PostCSS": "8.5.6",
    "Lucide React": "0.554.0 (Icons)"
  },
  "interaction": {
    "react-rnd": "10.5.2 (Drag & Resize)"
  },
  "utilities": {
    "uuid": "13.0.0",
    "localStorage": "Browser Native"
  },
  "development": {
    "ESLint": "9.39.1",
    "TypeScript ESLint": "8.46.4"
  }
}
```

### アーキテクチャの特徴

**1. Single Page Application (SPA)**
- クライアント側のみでルーティング実装
- ページリロード不要な快適な遷移

**2. Context API による状態管理**
- グローバル状態を一元管理
- Props drilling を回避
- `AppContext.tsx` が全データを管理

**3. イミュータブル状態更新**
```typescript
// 状態更新時は必ず新しいオブジェクトを生成
const updatedProjects = projects.map(p =>
  p.id === projectId
    ? { ...p, placedItems: [...p.placedItems, newItem] }
    : p
);
setProjects(updatedProjects);
```

**4. Drag & Drop の実装**
- `DataTransfer API` でドラッグソース・ターゲット管理
- `react-rnd` でドラッグとリサイズを統合
- `bounds="parent"` で親要素内に制限

**5. localStorage による永続化**
- プロジェクトデータ: `mbs_app_data_projects`
- IP資産データ: `mbs_app_assets`
- 人メモデータ: `mbs_app_person_memos` (NEW)
- 会社メモデータ: `mbs_app_company_memos` (NEW)
- チャットメッセージ: `mbs_app_messages` (NEW)
- ページリロード時に自動復元
- 破損データは自動的にデモデータへフォールバック

---

## 📁 ディレクトリ構成

```
MSP/
├── src/
│   ├── main.tsx                          ← アプリケーション起動点
│   ├── App.tsx                           ← ルーティング定義（/ と /event/:id）
│   │
│   ├── pages/                            ← ページコンポーネント
│   │   ├── Dashboard.tsx                 ← プロジェクト一覧画面
│   │   └── EventCanvas.tsx               ← キャンバス（コア機能）
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   └── Sidebar.tsx               ← IP資産 + 人メモ + 会社メモサイドバー
│   │   │
│   │   ├── canvas/                       ← キャンバス上の配置要素
│   │   │   ├── EventStickyCard.tsx       ← プロジェクトカード
│   │   │   ├── DraggableIpSticky.tsx     ← IP ノート（黄色）
│   │   │   ├── DraggableIdeaSticky.tsx   ← アイデア ノート（ピンク）
│   │   │   ├── DraggablePersonSticky.tsx ← 人メモ ノート（青色）NEW
│   │   │   └── DraggableCompanySticky.tsx ← 会社メモ ノート（緑色）NEW
│   │   │
│   │   ├── modals/                       ← モーダルコンポーネント NEW
│   │   │   ├── PersonMemoModal.tsx       ← 人メモ詳細情報表示
│   │   │   ├── CompanyMemoModal.tsx      ← 会社メモ詳細情報表示
│   │   │   └── ChatModal.tsx             ← オープンチャットスペース
│   │   │
│   │   └── ui/                           ← 汎用UI部品
│   │       ├── Modal.tsx                 ← ダイアログ/モーダル（サイズ対応）
│   │       └── Avatar.tsx                ← ユーザーアバター（色付き）
│   │
│   ├── context/
│   │   └── AppContext.tsx                ← グローバル状態管理 + localStorage
│   │
│   ├── data/                             ← モックデータ
│   │   ├── masterData.ts                 ← 12個のIP資産定義
│   │   ├── masterPersonData.ts           ← 5人の人メモデータ NEW
│   │   ├── masterCompanyData.ts          ← 5社の会社メモデータ NEW
│   │   └── demoProjects.ts               ← 5つのデモイベント案
│   │
│   ├── types/
│   │   └── index.ts                      ← TypeScript 型定義
│   │
│   └── assets/
│       ├── index.css                     ← Tailwind imports
│       └── App.css                       ← 追加カスタムスタイル
│
├── public/
│   └── assets/                           ← ロゴ画像（IP資産用）
│       ├── logo_yantan.png
│       ├── logo_prebato.png
│       ├── logo_gobugobu.png
│       └── ... (他のロゴ)
│
├── vite.config.ts                        ← Vite ビルド設定
├── tsconfig.app.json                     ← TypeScript 設定
├── tailwind.config.js                    ← Tailwind CSS 設定
├── package.json                          ← NPM 依存関係
└── README.md                             ← プロジェクト説明
```

---

## 🔄 データフロー

### 状態管理フロー

```
ユーザー操作
    ↓
EventCanvas.tsx (イベント画面)
    ↓
AppContext (Context API)
    ├─ projects: EventProject[] (イベント一覧)
    ├─ ipAssets: IpAssetMaster[] (IP資産)
    └─ setProjects / setIpAssets (状態更新)
    ↓
localStorage (ブラウザストレージ)
    ├─ mbs_app_data_projects
    └─ mbs_app_assets
    ↓
ページリロード時 自動復元
```

### ユーザー操作フロー

```
1. Dashboard.tsx にアクセス
   ↓ (プロジェクト一覧表示)
2. イベントカードをクリック
   ↓ React Router で /event/{projectId} に遷移
3. EventCanvas.tsx が読み込まれる
   ↓ AppContext から該当プロジェクトを取得
4. Sidebar.tsx からIP資産をドラッグ
   ↓
5. キャンバスにドロップ（DraggableIpSticky 生成）
   ↓
6. AppContext の placedItems に追加
   ↓
7. localStorage に自動保存
   ↓
8. ページリロード時も状態が保持される
```

---

## 💾 データ構造

### IpAssetMaster（IP資産定義）
```typescript
interface IpAssetMaster {
  id: string;              // "ip_yantan" など
  name: string;            // "ヤングタウン"
  category: string;        // "番組IP" | "キャラクター/タレント" | "技術/リソース"
  ownerName: string;       // "ラジオ制作部 田中太郎"
  contact: string;         // "yamada@mbs.jp"
  imagePath: string;       // "logo_yantan.png"
  iconName?: string;       // lucide-react アイコン名
}
```

### PersonMemo（人メモ - マスターデータ）NEW
```typescript
interface PersonMemo {
  id: string;              // "person_suzuki"
  name: string;            // "鈴木太郎"
  department: string;      // "グッズ制作部"
  expertise: string[];     // ["グッズ制作", "ブランディング"]
  email: string;           // "suzuki.taro@mbs.co.jp"
  phone?: string;          // "090-1234-5678"
  pastProjects: string[];  // ["ごぶごぶ FES", "ヤンタン周年グッズ"]
  avatar?: string;         // "avatar_suzuki.png"
}
```

### CompanyMemo（会社メモ - マスターデータ）NEW
```typescript
interface CompanyMemo {
  id: string;              // "company_design_co"
  name: string;            // "〇〇デザイン"
  specialty: string[];     // ["グラフィックデザイン", "Web制作"]
  pastProjects: string[];  // ["ごぶごぶ FES", "ラジオステーション番組"]
  pointOfContact: {        // 推薦者・担当者（複数可）
    personId?: string;
    name: string;
    email: string;
    role?: string;         // "グッズ制作部"
  }[];
}
```

### Message（チャットメッセージ）NEW
```typescript
interface Message {
  id: string;              // "msg_1234567890_xxx"
  memoType: 'person' | 'company';  // メモの種類
  memoId: string;          // PersonMemo.id または CompanyMemo.id への参照
  author: string;          // メッセージ著者名
  content: string;         // メッセージ本文
  timestamp: Date;         // タイムスタンプ
  reactions?: string[];    // リアクション（将来拡張用）
}
```

### EventProject（イベントプロジェクト）
```typescript
interface EventProject {
  id: string;              // "event_yantan_60"
  title: string;           // "ヤンタン 60周年 大感謝祭"
  location: string;        // "京セラドーム大阪"
  date: string;            // "2025-12-01"
  status: string;          // "募集中" | "企画中" | "準備中" | "終了"
  placedItems: PlacedItem[]; // 配置されたノート一覧
}
```

### PlacedItem（配置されたアイテム - Union型）
```typescript
type PlacedItem = PlacedIpItem | PlacedIdeaItem | PlacedPersonMemoItem | PlacedCompanyMemoItem;

// IP資産ノート
interface PlacedIpItem {
  type: 'ip';
  uniqueId: string;        // UUID
  assetId: string;         // IpAssetMaster.id への参照
  author: string;          // 追加者名
  position: { x: number; y: number };
  size: { width: string | number; height: string | number };
  zIndex: number;
  note?: string;           // ユーザーが入力したメモ
}

// アイデアノート
interface PlacedIdeaItem {
  type: 'idea';
  uniqueId: string;        // UUID
  text: string;            // 自由形式テキスト
  author: string;          // 著者名
  position: { x: number; y: number };
  size: { width: string | number; height: string | number };
  zIndex: number;
}

// 人メモノート NEW
interface PlacedPersonMemoItem {
  type: 'person';
  uniqueId: string;        // UUID
  memoId: string;          // PersonMemo.id への参照
  author: string;          // 配置した人の名前
  position: { x: number; y: number };
  size: { width: string | number; height: string | number };
  zIndex: number;
}

// 会社メモノート NEW
interface PlacedCompanyMemoItem {
  type: 'company';
  uniqueId: string;        // UUID
  memoId: string;          // CompanyMemo.id への参照
  author: string;          // 配置した人の名前
  position: { x: number; y: number };
  size: { width: string | number; height: string | number };
  zIndex: number;
}
```

---

## 🎨 UI/UX デザイン

### カラースキーム

| 要素 | 色 | Tailwind クラス |
|------|-----|-----------------|
| IP ノート | 黄色 | `bg-yellow-200` |
| アイデア ノート | ピンク | `bg-pink-200` |
| 人メモ ノート | 青色 | `bg-blue-200` (NEW) |
| 会社メモ ノート | 緑色 | `bg-emerald-200` (NEW) |
| ステータス: 募集中 | 緑 | `bg-green-100` |
| ステータス: 企画中 | 黄 | `bg-yellow-100` |
| ステータス: 準備中 | 青 | `bg-blue-100` |
| ステータス: 終了 | グレー | `bg-gray-100` |
| キャンバス背景 | 淡い灰青 | ドット柄パターン |

### インタラクティブな要素

**マウスホバー時:**
- スケーリング: `hover:scale-105`
- シャドウ強調: `hover:shadow-lg`
- スムーズ遷移: `transition-all duration-200`

**ノート視覚効果:**
- 回転: `-rotate-1` 〜 `rotate-0`（自然な配置感）
- シャドウ: `shadow-md`（立体感）

**アバター:**
- 著者名から決定論的に色をハッシュ
- 同じ著者は常に同じ色（状態管理不要）

---

## 🚀 NPM スクリプト

```bash
npm run dev       # 開発サーバー起動 (Vite HMR 有効)
npm run build     # 本番ビルド (型チェック + minify)
npm run lint      # ESLint でコード検査
npm run preview   # 本番ビルドをプレビュー
```

---

## 🔐 セキュリティ & 制限事項

### セキュリティ上の考慮

**デモアプリケーションのため以下の機能は実装されていません:**

- ❌ 認証・認可（ユーザー管理なし）
- ❌ HTTPS/TLS通信
- ❌ データ暗号化（localStorage は平文）
- ❌ バックアップ機能
- ❌ 複数ユーザー間の同期
- ❌ API レート制限
- ❌ 監査ログ

**想定される使用環境:**
- 内部ネットワーク環境でのデモンストレーション
- 限定されたプレゼンテーション用途
- 本番運用は想定していません

### ブラウザの制限

- localStorage の容量: ブラウザごとに異なる（通常 5-10 MB）
- キャッシュクリア時にデータ損失
- 複数タブでの同期は不完全（Context API のため）

---

## 📝 サンプルデータ

### メインイベント例

**「ヤンタン 60周年 大感謝祭」**
- イベント ID: `event_yantan_60`
- ステータス: 募集中
- 初期配置: ヤングタウン IP + アイデアメモ 2 件

### IP資産の例

| ID | 名前 | カテゴリ | 所有部門 |
|----|----|-------|--------|
| `ip_yantan` | ヤングタウン | 番組 IP | ラジオ局 |
| `ip_prebato` | プレバト！！ | 番組 IP | テレビ局 |
| `ip_gundam` | ガンダム | キャラクター | アニメ部 |
| `ip_studio` | スタジオサービス | 技術/リソース | エンタメ技術 |

---

## 🔄 今後の拡張想定

### 実装済み（Phase 1-4）
- ✅ 人メモ管理とドラッグ&ドロップ配置
- ✅ 会社メモ管理とドラッグ&ドロップ配置
- ✅ 人・会社メモの詳細情報モーダル
- ✅ オープンチャット機能（メモごと）
- ✅ localStorage による永続化

### 今後の拡張想定（Phase 5+）
- **削除前の確認ダイアログ** - UX改善（現在はワンクリック削除）
- **人メモ・会社メモの追加・編集UI** - サイドバー内での管理画面
- **メッセージのリアクション機能** - 👍 など
- **タグ/カテゴリ検索機能** - メモの高度な検索
- **「Aさんが関わったプロジェクト」ダッシュボード** - 人物ベースの可視化
- **実績ベースの推薦機能** - AI的な提案機能
- **バックエンド API** - Node.js/Express または他フレームワーク
- **データベース** - PostgreSQL / MongoDB 等
- **認証システム** - OAuth / JWT
- **リアルタイム同期** - WebSocket / Firebase
- **マルチユーザー対応** - 同時編集の競合処理
- **Export機能** - PDF / PNG でのプレゼン資料生成
- **通知機能** - イベント参加者への通知
- **メモのアーカイブ・復元機能** - 削除したメモの復元
- **チャットの全文検索** - メッセージ検索

---

## 📊 実装サマリー（Phase 1-4）

### 追加ファイル一覧（7ファイル）

```
/src/data/
├── masterPersonData.ts           ← 5人の人メモマスターデータ
└── masterCompanyData.ts          ← 5社の会社メモマスターデータ

/src/components/canvas/
├── DraggablePersonSticky.tsx     ← 人メモ付箋（青色）
└── DraggableCompanySticky.tsx    ← 会社メモ付箋（緑色）

/src/components/modals/
├── PersonMemoModal.tsx           ← 人メモ詳細モーダル
├── CompanyMemoModal.tsx          ← 会社メモ詳細モーダル
└── ChatModal.tsx                 ← オープンチャットモーダル
```

### 拡張・修正ファイル一覧（4ファイル）

```
/src/types/index.ts
  → PersonMemo, CompanyMemo, Message, PlacedPersonMemoItem, PlacedCompanyMemoItem 型追加

/src/context/AppContext.tsx
  → personMemos, companyMemos, messages 状態追加
  → addMessage, deleteMessage, getMessagesForMemo ユーティリティ追加
  → 3つの新しい localStorage キー実装

/src/components/layout/Sidebar.tsx
  → 人メモセクション追加（青色、ドラッグ可能）
  → 会社メモセクション追加（緑色、ドラッグ可能）
  → セクションの開閉管理ロジック追加

/src/pages/EventCanvas.tsx
  → DraggablePersonSticky, DraggableCompanySticky インポート追加
  → PersonMemoModal, CompanyMemoModal, ChatModal インポート追加
  → handleDrop 拡張（JSON ドラッグデータ対応）
  → 4ノート種別の rendering 対応
  → チャット状態管理追加
```

### AppContext の新機能

```typescript
// 新しい State
personMemos: PersonMemo[]
companyMemos: CompanyMemo[]
messages: Message[]

// 新しい Setter
setPersonMemos: Dispatch<SetStateAction<PersonMemo[]>>
setCompanyMemos: Dispatch<SetStateAction<CompanyMemo[]>>
setMessages: Dispatch<SetStateAction<Message[]>>

// 新しい Utility Functions
addMessage(memoType, memoId, author, content): void
deleteMessage(messageId): void
getMessagesForMemo(memoType, memoId): Message[]
```

### データフロー（新規）

```
ユーザー操作
    ↓
Sidebar.tsx (人メモ/会社メモセクション)
    ↓ (ドラッグイベント)
EventCanvas.tsx (handleDrop JSON パース)
    ↓
AppContext (personMemos/companyMemos 参照)
    ↓
DraggablePersonSticky / DraggableCompanySticky (描画)
    ↓
PersonMemoModal / CompanyMemoModal (クリック時)
    ↓
ChatModal (チャット開く)
    ↓
AppContext (addMessage via useContext)
    ↓
localStorage (mbs_app_messages)
```

---

## 📞 問い合わせ・改善

このアプリケーションはデモンストレーション用です。

フィードバックや改善提案があれば、プロジェクトメンテナーまでお問い合わせください。
